# This is the main configuration file for the application.
# https://www.playframework.com/documentation/latest/ConfigFile
play.http.secret.key = "320395e41b98f66c8fb473e8e9f12747bd791f6a841b949fd3224dd848e28569"
play.filters {
  enabled += "play.filters.cors.CORSFilter"
  cors {
    allowedOrigins = ["localhost"]
    allowedHttpMethods = ["GET", "POST", "PUT", "DELETE", "OPTIONS"]
  }
  hosts {
    # Allow requests to example.com, its subdomains, and localhost:9000.
    allowed = [".herokuapp.com", "localhost"]
  }
}
play.editor="http://localhost:63342/api/file/?file=%s&line=%s"

flux.mongodb.uri = "mongodb://localhost:27017"   # Default mongodb uri
flux.mongodb.uri = ${?MONGO_URI}  # Overrides the default URL with one from the environment variables (MONGO_URI)